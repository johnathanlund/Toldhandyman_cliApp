import { MyModalComponent } from './myModal.component';

describe('MyModalComponent', () => {
  it('should create an instance', () => {
    const component = new MyModalComponent();
    expect(component).toBeTruthy();
  });
});
